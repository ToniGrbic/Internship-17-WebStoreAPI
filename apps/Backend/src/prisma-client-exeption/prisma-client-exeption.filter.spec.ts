import { PrismaClientExeptionFilter } from './prisma-client-exeption.filter';

describe('PrismaClientExeptionFilter', () => {
  it('should be defined', () => {
    expect(new PrismaClientExeptionFilter()).toBeDefined();
  });
});
